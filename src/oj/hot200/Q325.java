package oj.hot200;

import java.util.Arrays;

public class Q325 {
    public int maxSubArrayLen(int[] nums, int k) {
        int res = -1;
        Arrays.sort(nums);
        int i = 0, j = 0, sum = 0;
        while (j < nums.length) {

        }

        return res;
    }
}
